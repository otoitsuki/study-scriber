# StudyScriber Frontend Environment Variables
# 後端 API 基礎 URL
NEXT_PUBLIC_API_URL=internal

# WebSocket 基礎 URL
NEXT_PUBLIC_WS_URL=ws://localhost:8000


# 開發模式配置
NODE_ENV=development


# 逐字稿顯示間隔
NEXT_PUBLIC_TRANSCRIPT_LABEL_INTERVAL=30

# 音訊切片間隔配置
# 方法一：直接設定毫秒數 (優先級較高)
# NEXT_PUBLIC_AUDIO_CHUNK_INTERVAL_MS=15000

# 方法二：設定秒數 (會自動轉換為毫秒)
NEXT_PUBLIC_AUDIO_CHUNK_INTERVAL_SEC=30

# 新增：設定音訊切片長度與重疊
NEXT_PUBLIC_AUDIO_CHUNK_DURATION_SEC=30
NEXT_PUBLIC_AUDIO_CHUNK_OVERLAP_SEC=0


# 如果都未設定，預設為 15 秒 (15000ms)
