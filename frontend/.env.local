# StudyScriber Frontend Environment Variables
# 後端 API 基礎 URL
NEXT_PUBLIC_API_URL=internal

# WebSocket 基礎 URL
NEXT_PUBLIC_WS_URL=ws://localhost:8000


# 開發模式配置
NODE_ENV=development


# ============================================  
# 注意：音頻切片與時間戳配置已統一由後端 .env 管理
# 前端會自動從 /api/config 讀取後端配置
# 請直接修改主目錄的 .env 檔案中的 AUDIO_CHUNK_DURATION_SEC
# ============================================

# 以下設定已棄用，僅作為後備值
# NEXT_PUBLIC_TRANSCRIPT_LABEL_INTERVAL=15
# NEXT_PUBLIC_AUDIO_CHUNK_INTERVAL_SEC=15  
# NEXT_PUBLIC_AUDIO_CHUNK_DURATION_SEC=15
# NEXT_PUBLIC_AUDIO_CHUNK_OVERLAP_SEC=0
